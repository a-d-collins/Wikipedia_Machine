A Pen created at CodePen.io. You can find this one at ____.

Wikipedia_Machine_v.1

1.0:
-- Goals:
    1. USE AS MUCH HTML AND CSS AS POSSIBLE
    2. ALSO USE AS LITTLE JS AS POSSIBLE
    3. THERE'S SKILL IN BEING SUCCINCT
    4. Expand div and input field on :hover
    5. By default, the .searchButton is NOT clickable (i.e. does not have class 'clickable')
    5. If input field is empty, fade .searchButton
    6. If input field is already populated, add class 'clickable' to .searchButton